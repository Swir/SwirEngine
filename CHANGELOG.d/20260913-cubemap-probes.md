### Added
- Add a validated six-face `CubemapFaces` asset contract, deterministic RGB cubemap payload loading and `ImageBasedEnvironment3D` scene probe settings as the CPU/API foundation for upcoming GPU cubemap/IBL rendering.
- Add deterministic cubemap asset discovery helpers for live-development and renderer integration.
