### Added

- Added the first SwirEngine 2.2 Particle/VFX Editor slice: deterministic project VFX assets, CPU 2D / GPU 3D shipping-runtime bridges, creator presets and bounded runtime preview controls.
- Added project-scoped VFX texture validation and creator diagnostics without changing the 5/10 accepted roadmap state.

### Safety and performance

- Preview stepping is bounded to avoid unbounded editor catch-up, and project texture references fail closed when they escape the project asset root.
