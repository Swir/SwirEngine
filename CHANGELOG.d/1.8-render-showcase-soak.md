### SwirEngine 1.8 — render showcase, soak and failure injection

- Added a bounded deterministic render-soak harness that drives the Renderer2 1.8 compatibility
  bridge together with optional transient-resource and texture-upload workloads.
- Added frame-boundary failure containment with a hard consecutive-failure budget, bounded portable
  failure history, recovery accounting and deterministic workload fingerprints.
- Added source-only 2D/3D showcase and long-run workload gates, allocation/upload/backend failure
  regressions, Windows source coverage and Linux OpenGL smoke coverage without publishing a release.
