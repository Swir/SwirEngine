# SwirEngine 2.0 public API migration contract

- Added a machine-verifiable 2.0 compatibility floor anchored to the actual published `v1.5.0` root API blob, export count and deterministic export digest.
- Added an explicit 1.5.0 → 2.0 migration ledger and API-stability contract that requires deliberate breaking changes to ship with justification, migration evidence and regression coverage.
- Added a Python 3.10/3.13/3.14 Public API 2.0 CI gate with full Git history/tag validation and deterministic SWIR progress presentation checks.
- Kept package and module version metadata frozen at 1.5.0; no intermediate release, tag or PyPI publication is created.
