## 1.7 source development — Background Jobs & Main-thread Handoff

- started the SwirEngine 1.7 **Parallel Runtime & Asset Throughput** source-development roadmap from **0/10 = 0.0%**, with Background Jobs & Main-thread Handoff as the first verification candidate after the completed 1.6 source checkpoint
- added additive `swirengine.jobs17.JobScheduler` with bounded worker concurrency and a hard unfinished-job back-pressure budget
- added deterministic priority/FIFO ready-job dispatch, dependency gating, transitive dependency blocking and stable rejection codes
- hardened malformed dependency-container validation so unsupported runtime values are rejected atomically with `invalid_dependencies` instead of leaking an iteration `TypeError`
- added queued and cooperative running cancellation through `JobContext`, plus worker-failure isolation so unrelated work continues
- added bounded `drain_completed(max_items=...)` main-thread handoff, explicit completed-record forgetting and payload-free numeric diagnostics
- added focused tests, creator documentation/example, compile validation and a 2,000-job scheduler workload contract with a generous 5.0-second CI budget
- added dedicated Python 3.10/3.13/3.14 validation workflow
- kept published package metadata at 1.5.0; **Release/PyPI remain frozen until SwirEngine 2.0**
