### Added

- Added opt-in SwirEngine 1.5 `PerformanceDiagnostics2` frame/update/physics/render instrumentation with bounded history, subsystem counters, asset timing, explicit resource accounting and optional Python allocation snapshots.
- Added provider adapters for mapping/dataclass/`portable()` diagnostics so navigation, world streaming, physics and cache telemetry can be sampled without changing stable 1.x APIs.
- Added versioned deterministic performance capture JSON with canonical SHA-256 fingerprints and atomic export for regression-friendly CI artifacts.
- Added focused Python 3.10/3.13/3.14 validation, stable profiler regression coverage, a headless example and a 10,000-frame instrumentation workload gate.
