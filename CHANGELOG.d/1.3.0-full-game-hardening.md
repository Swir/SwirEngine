# Full Game + Hardening + Release Gate

- Added **Neon Frontier 1.3**, an asset-free integrated validation game that exercises the production OpenGL renderer, GPU instancing, `ShaderMesh3D`, 3D collision queries, 3D navigation, bounded large-world streaming and deterministic gameplay scheduling in one application.
- Added deterministic frame-count OpenGL smoke mode plus a packaged native-runtime probe for Windows validation.
- Added `tools/verify_1_3_release_candidate.py` with a development contract and a strict `--require-complete` publication contract requiring version 1.3.0 and exactly 10/10 roadmap completion.
- Added the dedicated Full Game 1.3 CI workflow with focused subsystem regressions, Ruff/compileall, real Xvfb/software-OpenGL execution and a one-file Windows PyInstaller probe.
- Updated normal CI to validate the active 1.3 release contract.
- Hardened the final release workflow around the 1.3 contract, deterministic performance regression gates, Neon Frontier runtime validation, Windows CPython 3.14 native-wheel selection and public-PyPI post-release verification.
- No FPS improvement is inferred from host timing; deterministic workload counters remain the release performance contract.
