# SwirEngine 2.0 final release gate candidate

- Add a machine-verifiable two-phase 2.0 release contract that keeps preflight at 9/10 with package metadata frozen at 1.5.0, then requires an exact-head 10/10 finalization before tagging.
- Add a dedicated final-gate workflow covering full repository regressions, the supported 15-cell CPython/OS matrix, exact-source packaging, host-native desktop shipping and real Linux OpenGL fixture smoke tests.
- Add immutable `v2.0.0` tag creation from an exact-main publication branch plus a tag-only PyPI/GitHub release workflow using Trusted Publishing.
- Add public PyPI post-release verification on Linux, macOS and Windows, including a clean outside-checkout import and Linux renderer smoke checks.
- Add final release documentation, release notes and regression tests while preserving the 90.0% roadmap state and the Release/PyPI freeze until the final gate is actually completed.
