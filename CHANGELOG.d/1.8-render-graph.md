### Added

- Added the additive `swirengine.render_graph18` planner for deterministic pass/resource dependency
  scheduling, rooted pass culling, strict graph validation, transient lifetime analysis, alias-slot
  planning, portable diagnostics and stable plan fingerprints.
- Added a creator demo, focused regression suite and a 1,200-pass planner workload gate for the new
  source-only SwirEngine 1.8 development line.
