### Added

- Added the additive `swirengine.render_resources18` transient backend resource pool with bounded
  object/byte residency, exact descriptor compatibility, generation-safe leases, deterministic idle
  reuse/eviction, explicit trim/close behavior and payload-free diagnostics.
- Added render-graph lifetime scheduling and pass-scoped pool sessions that consume verified
  `RenderGraphPlan` transient lifetimes without changing stable 1.x renderer behavior.
- Added focused tests, a creator demo and a deterministic 51,200-acquisition workload gate for the
  source-only SwirEngine 1.8 rendering scalability line.
