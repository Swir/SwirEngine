# Advanced Material & Shader Pipeline

- add immutable `ShaderTemplate`, explicit `ShaderHookPoint` seams and deterministic prepared `ShaderVariantSpec` keys
- add conservative hook/define safety validation so creator customization cannot replace engine-owned shader version/layout/state contracts through the variant API
- add bounded `ShaderProgramCache` LRU compilation caching with creator-visible hit/miss/failure/eviction/invalidation diagnostics
- add `ShaderMaterial3D` validated custom uniforms without changing existing `Material3D` behavior
- add regression coverage requiring 1 backend compile plus 999 cache hits across 1,000 resolves of one prepared variant
- add a 10,000-resolve benchmark contract, runnable creator example and focused documentation
- keep the 1.3 roadmap item open until production renderer integration and real OpenGL validation are complete
