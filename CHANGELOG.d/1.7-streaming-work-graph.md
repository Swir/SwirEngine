# SwirEngine 1.7 — Streaming Work Graph 3.0

- added additive `swirengine.work_graph17` staged streaming orchestration over the verified 1.7 background scheduler
- fixed phase affinity so prefetch/decode work stays on workers while instantiate/unload callbacks run only from explicit owning-thread `poll(...)` calls
- added deterministic dependency ordering, priority scheduling, bounded graph/submission/callback work, creator-visible progress diagnostics and payload-free portable counters
- added branch-local failure isolation plus explicit cascade/non-cascade cancellation semantics
- added creator documentation, runnable example, focused regression tests and a deterministic 2,048-node workload gate
- kept stable 1.x root imports and published SwirEngine 1.5.0 metadata unchanged
- Release/PyPI remain frozen until SwirEngine 2.0
