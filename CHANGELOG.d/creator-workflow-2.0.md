SwirEngine 2.0 Milestone 2 candidate: add an integrated creator project workflow that composes
manifest validation, deterministic run planning, scene/prefab packages, content build diagnostics,
editable shipping defaults, editor directories and save/profile policy into one actionable report.
`swirengine new` now creates the creator layout and editable input/settings defaults, while
`swirengine workflow` provides human/JSON inspection and safe idempotent preparation.
